Raw match data is not committed to this public repository.

To reproduce the notebook, place the original course CSV here:

```text
data/match_data_582_converted.csv
```

The notebook also accepts an absolute path through:

```bash
export MATCH_DATA_CSV=/absolute/path/to/match_data_582_converted.csv
```
